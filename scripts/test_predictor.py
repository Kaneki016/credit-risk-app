"""DEPRECATED: helper moved to `tests/test_predictor.py`.

This file is intentionally left as a small pointer to the new test location so
users who previously ran `python scripts/test_predictor.py` are guided to use
`pytest` instead.

Run the test suite with:

    pytest -q

"""

print("NOTE: helper moved to tests/test_predictor.py — run `pytest -q` to execute it.")
